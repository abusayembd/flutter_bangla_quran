import 'package:flutter/material.dart';

class Subjective extends StatelessWidget {
  const Subjective({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
